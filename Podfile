# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyBeauty' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
    pod 'Firebase/Auth'
    pod 'Firebase/Database'
    pod 'Firebase/Storage'
    pod 'Firebase'
    pod 'SideMenu'
  # Pods for MyBeauty

  target 'MyBeautyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyBeautyUITests' do
    # Pods for testing
  end

end
